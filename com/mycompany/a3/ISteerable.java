package com.mycompany.a3;

//Interface for objects that can be moved(steered) 
public interface ISteerable {

	public void changeDirection(int newDirection);
	public void changeSpeed(int newSpeed);
}
